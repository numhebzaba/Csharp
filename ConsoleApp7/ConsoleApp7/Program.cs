using System;

namespace ConsoleApp7
{
    class Program
    {
        static void Main(string[] args)
        {
            //พิ้นที่วงกลม
            //Console.Write("Input diameter ");
            //double diameter,radius;
            //diameter = double.Parse(Console.ReadLine());
            //radius = diameter / 2;

            //double circleArea = (22.0 / 7) * Math.Pow(radius, 2);
            //Console.WriteLine("Circle Area : "+circleArea);



            //พื้นที่สี่เหลียวคางหมู
            //double length1 = double.Parse(Console.ReadLine());
            //double length2 = double.Parse(Console.ReadLine());
            //double height = double.Parse(Console.ReadLine());

            //double Area = 0.5 * (length1 + length2) * height;
            //Console.WriteLine(" Area : " + Area);



            //พื้นที่หกเหลี่ยมด้านเท่า
            //double length1 = double.Parse(Console.ReadLine());
            //double Area = (3 * Math.Sqrt(3) * Math.Pow(length1, 2))/2;

            //สูตรหา X
            //double a = double.Parse(Console.ReadLine());
            //double b = double.Parse(Console.ReadLine());
            //double c = double.Parse(Console.ReadLine());

            //double x1 = (-b + Math.Sqrt(Math.Pow(b, 2) - (4.00 * a * c)))/(2*a);
            //double x2 = (-b - Math.Sqrt(Math.Pow(b, 2) - (4.00 * a * c))) / (2*a);
            //Console.WriteLine(x1);
            //Console.WriteLine(x2);


        }
    }
}
